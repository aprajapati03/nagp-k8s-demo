apiVersion: v1
kind: Service
metadata:
  namespace: nagp-k8s-demo
  name: db-service
  labels:
    app: employee-app
    tier: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: db-tier
